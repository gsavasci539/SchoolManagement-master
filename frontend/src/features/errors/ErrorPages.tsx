import { ArrowLeft, Home } from "lucide-react";
import { Link, useNavigate } from "react-router-dom";

export function ForbiddenPage() { const navigate = useNavigate(); return <div className="error-page"><div><div className="error-code">403</div><h1>Bu alan için yetkiniz yok</h1><p>Hesabınız bu sayfayı görüntüleme iznine sahip değil. Erişim gerektiğini düşünüyorsanız kurum yöneticinizle görüşün.</p><div className="inline" style={{ justifyContent: "center", gap: 9 }}><button className="btn btn-secondary" onClick={() => navigate(-1)}><ArrowLeft size={16} />Geri dön</button><Link className="btn btn-primary" to="/dashboard"><Home size={16} />Ana sayfa</Link></div></div></div>; }
export function NotFoundPage() { const navigate = useNavigate(); return <div className="error-page"><div><div className="error-code">404</div><h1>Aradığınız sayfa bulunamadı</h1><p>Bağlantı değişmiş veya sayfa kaldırılmış olabilir. Menüden güvenli bir alana geçebilirsiniz.</p><div className="inline" style={{ justifyContent: "center", gap: 9 }}><button className="btn btn-secondary" onClick={() => navigate(-1)}><ArrowLeft size={16} />Geri dön</button><Link className="btn btn-primary" to="/dashboard"><Home size={16} />Ana sayfa</Link></div></div></div>; }
